version: '2'
services:
  app:
    working_dir: /app
    image: node
    env_file:
        - env
    volumes:
      - ./app:/app
    security_opt:
     - seccomp:unconfined
    entrypoint: bash -c "npm install && npm run dev"
    ports:
     - "3000:3000"
  api:
    build: ./api
    env_file:
        - env
    security_opt:
     - seccomp:unconfined
    ports:
     - "3001:3001"